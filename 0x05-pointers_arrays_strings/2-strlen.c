#include "main.h"

/**
 * _strlen - returns the lenght of a string
 * @s: string
 * return: length of a string
 */
int _strlen(char *s)
{
	int len = 0;

	while (*s + != '\0')
	{
		len++;
		s++;
	}
	return (lenght);
}
